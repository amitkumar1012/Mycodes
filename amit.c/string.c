#include<stdio.h>
#include<string.h>
int main()
{
    char s1[10]; int i,vowel=0;
    printf("Enter First String:");
    fgets(s1,10,stdin);
    for(i=0; s1[i];i++)
    {
        if(s1[i]=='a||e||i||o||u||A||E||I||O||U]');
        vowel +=1;

    }

    
    printf("%d", vowel);
    return 0;
}